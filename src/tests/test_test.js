/* eslint-env mocha */

const expect = require('chai').expect

describe('Try mocha', () => {
  it('should create the cache directory when instantiated', () => {
    expect(true).to.equal(true)
  })
})
